
<div class="row">
  {{#each products}}
    <div class="col-12 col-md-6 border mb-2 mb-md-0">
      <div class="row product-row">
        <div class="col-4">
          <div class="image">
            <img src="{{this.imagePath}}" alt="Image">
          </div>
        </div>
        <div class="col-6 product-info">
          <h4>{{this.title}}</h4>
          <div>
           {{this.description}}
          </div>
        </div>
      </div>
      <div class="d-flex align-items-center justify-content-end">
        <b class="mr-2">${{this.price}}</b>
        <button type="button" class="btn btn-success">Köp</button>
      </div>
    </div>
  {{/each}}
</div>
    <!-- <div class="col-12 col-lg-4">
      <div class="card product-card">
        <img class="card-img-top" src="{{this.imagePath}}" alt="Card image cap">
        <div class="card-body d-flex flex-column h-100">
          <h5 class="card-title">{{this.title}}</h5>
          <p class="card-text">{{this.description}}</p>
          <div class="mt-auto d-flex justify-content-between flex-wrap">
            <p class="">${{this.price}}</p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
          </div>
        </div>
      </div>
    </div> -->